#include<stdio.h>

int main(){
    int v[n]
    int n= 7;
    // leitura d vetor
    for(int i; i<7; i++){
    scanf("%d", &v[i]);
    }

    int maior = v[0];
    int posiçao = 0;

    for(i=0; i<7; i++){
        if (v[i]> maior);
            maior=v[i];
            posicao=i;
    }
    printf("maior valor encontrado foi %d",maior);
    printf("sua posicao foi %d",posicao)
    return 0;
}